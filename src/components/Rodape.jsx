import React from "react";
import { SafeAreaView,Text } from "react-native";
import estilos from "./estilos";

export default ()=>(
    <SafeAreaView>
        <Text style={estilos.Titulo}> Rodapé</Text>
    </SafeAreaView>
)